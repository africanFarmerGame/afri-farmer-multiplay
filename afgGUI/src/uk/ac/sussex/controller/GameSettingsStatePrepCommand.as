package uk.ac.sussex.controller {	import org.puremvc.as3.multicore.interfaces.*;	import org.puremvc.as3.multicore.patterns.command.*;		import uk.ac.sussex.general.ApplicationFacade;	import uk.ac.sussex.states.GameSettingsGameState;	import uk.ac.sussex.model.GameProxy;		public class GameSettingsStatePrepCommand extends SimpleCommand {		override public function execute ( note:INotification ):void {			var gameProxy:GameProxy = facade.retrieveProxy(GameProxy.NAME) as GameProxy;			if(gameProxy == null){				//We're looking at a new game, so create a blank one. 				facade.registerProxy(new GameProxy());			}						sendNotification(ApplicationFacade.CHANGE_STATE, GameSettingsGameState.NAME);		}	}	}